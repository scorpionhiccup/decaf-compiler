# Decaf Compiler

### Instructions to run the code
1. Run `make` command.
2. To run the Compiler, run the command `./syntax_analyser <prog_file>`. If prog_file is not specified, stdin as taken as input. 